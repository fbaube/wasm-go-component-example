go get -tool go.bytecodealliance.org/cmd/wit-bindgen-go
# The -tool flag instructs go to add a matching 
# tool line to go.mod for each listed package. 
# If -tool is used with @none, the line is removed.
ls -l go.???
